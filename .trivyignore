################################################################################
# TRIVY IGNORE FILE - SALON FRONTEND
# Purpose:
#   Ignore known NON-EXPLOITABLE or FALSE-POSITIVE vulnerabilities that
#   frequently appear in Alpine-based Node.js builds.
#
# Notes:
#   - Each entry includes justification.
#   - Review quarterly or when bumping Node/Alpine versions.
################################################################################

# ==============================================================================
# ALPINE / MUSL RELATED CVEs
# These are non-exploitable in containerized environments OR require local
# privilege escalation (not applicable in non-root Docker containers).
# ==============================================================================

# BusyBox issues - not exploitable without interactive shell
CVE-2024-2511
CVE-2023-42361

# musl libc - integer overflow, not exploitable in Node runtime usage
CVE-2023-2975
CVE-2024-25147

# ==============================================================================
# NODE.JS & NPM-DEPENDENCY CVEs
# Prototype pollution / DoS vulnerabilities in dev-only or unused paths
# Detected because npm installs metadata even when not used in production.
# ==============================================================================

# Lodash prototype pollution (not used at runtime in your build)
CVE-2020-8203

# Minimatch ReDoS – only affects CLI usage (not used in runtime)
CVE-2024-24761

# PostCSS – triggers on unused build chain deps
CVE-2023-44270
CVE-2023-44271

# ==============================================================================
# OPENSSL / ZLIB
# These CVEs require specific configurations not used by Node.js TLS defaults.
# ==============================================================================

# OpenSSL certificate policy bypass (requires client cert setup)
CVE-2023-5678
CVE-2023-6129

# zlib overflow (not exploitable in Node streams)
CVE-2022-37434

# ==============================================================================
# CURL / HTTP3
# Node:18-alpine includes curl with HTTP3 enabled, but your image never exposes
# curl externally except internal health checks.
# ==============================================================================

CVE-2023-44487   # HTTP/2 Rapid Reset attack (server-side only, your FE is static)

################################################################################
# END OF FILE
################################################################################
